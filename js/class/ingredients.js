class ingredients {
    constructor(idIngr, nameIngr, isSpice, isVegetable) {
        this.idIngr = idIngr;
        this.nameIngr = nameIngr;
        this.isSpice = isSpice;
        this.isVegetable = isVegetable;
        this.isAvailable = true;
    }
}